# =============================================================================
# .env.example — PLANTILLA DE CONFIGURACION
# Copiar a .env y completar con valores reales ANTES de levantar el sistema.
# NUNCA commitear el archivo .env al repositorio.
# =============================================================================

# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_USER=moly
# CAMBIAR: usar una password larga y aleatoria en produccion
# Generar con: python -c "import secrets; print(secrets.token_urlsafe(24))"
POSTGRES_PASSWORD=CHANGEME_strong_db_password_here
POSTGRES_DB=molymarket

# ── Backend / FastAPI ─────────────────────────────────────────────────────────
# CAMBIAR: clave secreta para firmar JWT, minimo 32 caracteres
# Generar con: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGEME_generate_with_secrets_token_hex_32

# Entorno: development | staging | production
ENVIRONMENT=development

# ── API Keys externas (opcionales) ────────────────────────────────────────────
GOOGLE_MAPS_API_KEY=
OPENAI_API_KEY=
ORS_API_KEY=
MAPBOX_TOKEN=

# ── CORS: origenes permitidos (separados por coma en produccion) ──────────────
ALLOWED_ORIGINS=http://localhost:3000

# ── Frontend / Next.js ────────────────────────────────────────────────────────
# URL publica del backend (debe ser accesible desde el browser del usuario)
# En produccion: https://api.tudominio.com/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
